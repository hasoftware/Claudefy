# Auto-detect text/binary
* text=auto

# Shell scripts: always LF (required for bash on Linux/macOS)
*.sh text eol=lf

# Windows scripts: CRLF
*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf

# Docs: keep native, but treat as text
*.md text
LICENSE text
